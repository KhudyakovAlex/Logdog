services:
  ingest:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      LOGDOG_DB_PATH: /data/logdog.db
      LOGDOG_DB_MAX_BYTES: "1073741824"
      LOGDOG_HTTP_MAX_BYTES: "4194304"
    volumes:
      - ./data:/data
